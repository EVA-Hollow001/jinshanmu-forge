# 金山木攻坚 · Collatz · 第 3 轮收口报告

状态：铺路 S1/S2/S3 齐。拆台进行中。未交卷。

---

## 一句话

第 3 轮没有新证明通道。夹逼在 3.2 窗内 **零杀伤**；计算线还能再啃一层 \(m=190537\)（约 3 天扫描）；再往上必须换工具。

---

## S1 — R1 补证（存活，未升级为全量独立证书）

- \(m=5..10^5\) 共 99996 个 \(m\)：\(B_{\mathrm{exact}}=B_{\mathrm{bound}}\)，失败 0。+2 垫全程未用。
- \(N_{\max}=7{,}216{,}102{,}494\) @ \(m=79335\) 独立钉死。
- 独立 T-检测器：\(10^7..10^8\) 共 1500 万，0 环；\(N_{\max}\) 末端 200 万窗口 33.3 万，0 环。
- **未做**：全量 \(5..N_{\max}\) 独立重扫（过滤后约 \(1.20\times 10^9\)，本两段覆盖 ~1.3%）。全量 0 环仍绑在第 2 轮 `global_scan`。

## S2 — 合法 d/A 结构（R2a 保持死亡）

合法：\(D\le N_{\max}/5\)。窗内 \(m\ge 5\) **自动成立，零排除力**。

夹逼区间 \(I(m,A)\)：\(m=5..200\) 窗内 **1870 对，empty=0**。非法方向 \(D>N_{\min}/5\) 会误杀 **1818/1870**。

定点：\((6,10)/(15,24)/(15,25)\) 非空；负侧 \((2,3)/(7,11)\) 未误杀；\((3,5)\) 非空（C4 靠组合筛）。

多 A：\(m=5..10^5\) 候选 465,551,676；empty=1（m=7）；single=11；multi=99984。组合筛 \(\sum C(A-1,m-1)\approx 10^{49163}\)，不可行。

**真实搜索形状**：每 \(m\) 一个 A-窗，窗内每个 A 的 \(n_1\) 落在非空 \(I\)；排除引擎只剩最小元扫描与小 D 叶筛。

## S3 — 扫描上限

\(B(m,A_0)=\lfloor 1/(3(2^{d_0/m}-1))\rfloor\)。草稿分母 \(2^{d_0/m}-3\) 已死。

| 推到 M | N_max | 相对 1e5 | 单核粗估 |
|---|---|---|---|
| 1e5（已做） | 7.22e9 | ×1 | 已跑 |
| **190537** | **9.85e11** | **×136** | **~3 天** |
| 1.08e7 | 2.94e14 | ×40798 | ~2 年 |
| 1.72e8 单点 | ~3e16 | — | 百年级 |

中间 3e5 / 6.65e5 不涨上限。换工具点 = 越过 190537 还想盖住 10781274。

---

## 幸存者

1. R1 最小元扫描 + 全量 B 证书（周期 \(\le 10^5\) 无环，全量扫描未独立复跑）。
2. Baker / 夹逼单独无排除力（no-go + S2）。
3. R3：\(D\le 10^9\) 0 非平凡。
4. 第 1 轮：\(m=2,3,4,7\)；负侧过滤器。

## 被杀死 / 不再用

- A 唯一化；\(\{m\alpha\}>0.737\) 作为必要条件；「真实 \(f\le 0.2\)」
- 窗内夹逼直接排除
- 草稿 \(B\approx 1/(2^{d_0/m}-3)\)

## 下一步（第 4 轮分岔）

只剩两个可执行选项，不能两条都无限烧：

- **A. 工程**：扫到 \(m=190537\)（~3 天，上限 ×136）。仍远低于文献 \(10^{10}\)。
- **B. 收山**：写山穷报告——每条死路卡点 + 要闭合需要的定理（m-循环 \(\varphi(m)\) / 整除结构的新下界）。

推荐 **B**，除非主理人点名要烧 3 天扫 190537。
